Fixes #<issue_number_goes_here>

> It's a good idea to open an issue first for discussion.

## Summary of Changes

- **Application(s) affected:** <!-- e.g., FoldRun, Pharma on Gemini Enterprise, Sentinel, Docs -->
- **Description:**

## Contributor Checklist

- [ ] Google Contributor License Agreement (CLA) is signed (`cla/google` check passes)
- [ ] All new files include the standard Apache 2.0 license header (`addlicense -check .`)
- [ ] Python code is formatted and linted with Ruff (`ruff format --check . && ruff check .`)
- [ ] Frontend / Node code (if modified) passes lint and formatting checks (`npm run lint`)
- [ ] Terraform configs (if modified) are formatted (`terraform fmt -check -recursive`)
- [ ] Unit tests pass (`uv run --frozen pytest tests/unit`)
- [ ] Appropriate changes to documentation are included in the PR
